import { colors } from "./colors";
import { space } from "./spacing";
import { fonts, fontWeights, fontSizes } from "./fonts";

export const theme = {
  colors,
  space,
  fonts,
  fontSizes,
  fontWeights,
};
